<mah:MetroWindow x:Class="Dart.Common.UserInterface.PlugInsDialog.PlugInsDialog"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                 xmlns:properies="clr-namespace:Schuermann.Dart.App.Properties"
                 xmlns:mah="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
                 mc:Ignorable="d" WindowStartupLocation="CenterOwner"
                 Title="{x:Static properies:Resources.PlugInsDialogTitel}" Height="300" Width="600">
   <Grid>
      <Grid.RowDefinitions>
         <RowDefinition Height="*" />
         <RowDefinition Height="48" />
      </Grid.RowDefinitions>
      <ListBox Name="PlugIns" Margin="5,5,5,5">
         <ListBox.ItemTemplate>
            <DataTemplate>
               <TextBlock Text="{Binding}" FontSize="22" />
            </DataTemplate>
         </ListBox.ItemTemplate>
      </ListBox>
      <Button Content="{x:Static properies:Resources.Ok}" Grid.Row="1" Height="30" MinWidth="70" Margin="5,5,5,5" HorizontalAlignment="Right" Click="Ok_Click" />
   </Grid>


</mah:MetroWindow>