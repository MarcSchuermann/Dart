<UserControl x:Class="Dart.PlayerlistView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:Dart">

    <UserControl.Resources>
        <local:ListToVisibilityConverter x:Key="listToVisabilityConverter"/>
    </UserControl.Resources>

    <Grid>
        <ListView ItemsSource="{Binding Playerlist}" Visibility="{Binding Playerlist, Converter={StaticResource listToVisabilityConverter}}"  BorderThickness="0">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel>
                        <local:PlayerView DataContext="{Binding}" />
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</UserControl>